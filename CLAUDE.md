# Claude-specific entry point

Use `AGENTS.md` for repository-wide rules and `.ai-harness/` for structured workflows, capabilities, and run artifacts.

When the user asks for research, a proof of concept, or an adversarial review, use the corresponding harness capability instead of silently skipping it.
