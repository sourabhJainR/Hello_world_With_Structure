# Gemini CLI entry point

Use `AGENTS.md` for repository-wide rules and `.agents/skills/ai-coding-orchestrator/SKILL.md` for adaptive routing.

For non-trivial requests, determine the minimum required workflow before editing. Use `.ai-harness/run.py` when structured execution, validation, or learning is useful.